---
created: 2026-01-21
updated: 2026-01-21
categories: ["[[Architecture]]"]
project: "[[Projects/Urology-Data-Platform/CUA数据库平台|CUA数据库平台]]"
project_path: Projects/Urology-Data-Platform
subtype: architecture
status: active
people:
  - "[[姚志远]]"
tags: [设计, mvp, 架构]
---

# CUA数据库平台 - 设计与实施计划

## 概述

为泌尿外科医生打造的统一患者数据平台，聚合多模态医疗数据（首先从化验报告开始），并利用 AI 进行提取和摘要生成。

**核心问题**：泌尿外科医生需要花费大量时间从分散的系统中拼凑患者病史信息。

**解决方案**：统一的患者时间线视图，结合 AI 驱动的提取和摘要功能。

---

## MVP 范围

- **主要用户**：泌尿外科医生/外科医生
- **部署方式**：混合部署（本地数据，云端 AI）
- **规模**：1 家试点医院，1-5 名用户
- **合规性**：基础安全（认证 + 加密），正式合规推迟

### 功能

1. 手动上传化验报告（扫描图片 + 数字化 PDF）
2. VLM/OCR 提取为结构化 JSON
3. 按生命周期阶段组织的患者时间线视图
4. LLM 驱动的患者摘要生成
5. 跨患者记录的语义搜索

### MVP 范围外

- CT/超声/内镜支持
- EMR/PACS 集成
- 临床建议
- 多站点部署
- 完整 HIPAA 合规

---

## 系统架构

```mermaid
flowchart TB
    subgraph Hospital["🏥 医院内网（本地部署）"]
        subgraph Frontend["前端层"]
            UI["🖥️ Streamlit 前端<br/>患者管理 / 时间线 / 上传"]
        end
        
        subgraph Backend["后端层"]
            API["⚡ FastAPI 后端<br/>REST API + 后台任务"]
        end
        
        subgraph Storage["存储层"]
            PG["🐘 PostgreSQL<br/>元数据 / 用户 / 患者"]
            MV["🔍 Milvus<br/>向量嵌入 / 语义搜索"]
            MIO["📦 MinIO<br/>文件存储 / 化验报告"]
        end
        
        UI <-->|HTTP/JSON| API
        API --> PG
        API --> MV
        API --> MIO
    end
    
    subgraph Cloud["☁️ 云端服务"]
        Claude["🤖 Claude API<br/>VLM 提取 / LLM 摘要"]
    end
    
    API <-->|HTTPS| Claude
    
    style Hospital fill:#e8f5e9,stroke:#2e7d32
    style Cloud fill:#e3f2fd,stroke:#1565c0
    style Frontend fill:#fff3e0,stroke:#ef6c00
    style Backend fill:#fce4ec,stroke:#c2185b
    style Storage fill:#f3e5f5,stroke:#7b1fa2
```

---

## 数据模型

### 实体关系图

```mermaid
erDiagram
    Patient ||--o{ ClinicalEvent : has
    User ||--o{ ClinicalEvent : uploads
    ClinicalEvent ||--o| LabResult : extends
    
    Patient {
        uuid id PK
        string mrn UK
        string firstName
        string lastName
        date dateOfBirth
        string gender
        string hospitalId FK
        datetime createdAt
        datetime updatedAt
    }
    
    User {
        uuid id PK
        string username UK
        string email UK
        string passwordHash
        string role
        string hospitalId FK
        boolean isActive
        datetime createdAt
    }
    
    ClinicalEvent {
        uuid id PK
        uuid patientId FK
        enum phase
        enum eventType
        datetime eventDate
        string title
        text description
        uuid uploadedBy FK
        datetime createdAt
    }
    
    LabResult {
        uuid id FK
        string filePath
        string fileType
        enum status
        json extractedData
        float confidence
        string embeddingId
    }
```

### 字段说明

| 实体 | 关键字段 | 说明 |
|------|----------|------|
| **Patient** | `mrn` | 病历号（唯一） |
| | `hospital_id` | 所属医院 |
| **User** | `role` | 角色：urologist / nurse / admin |
| **ClinicalEvent** | `phase` | 生命周期阶段 |
| | `event_type` | 事件类型：化验报告、影像、手术、笔记 |
| **LabResult** | `file_type` | 文件类型：image / pdf |
| | `extraction_status` | 状态：pending / processing / completed / failed |

### 生命周期阶段

```mermaid
stateDiagram-v2
    [*] --> 问诊: CONSULTATION
    问诊 --> 术前: PRE_SURGERY
    术前 --> 手术: SURGERY
    手术 --> 术后: POST_SURGERY
    术后 --> 随访: FOLLOW_UP
    随访 --> [*]
    
    note right of 问诊: 初诊、检查
    note right of 术前: 术前准备、评估
    note right of 手术: 手术操作
    note right of 术后: 恢复观察
    note right of 随访: 长期跟踪
```

---

## API 设计（REST）

| 方法 | 端点 | 描述 |
|------|------|------|
| `POST` | `/api/auth/login` | JWT 认证登录 |
| `POST` | `/api/patients` | 创建患者 |
| `GET` | `/api/patients/{id}` | 获取患者详情 |
| `GET` | `/api/patients/{id}/timeline` | 获取患者临床事件时间线 |
| `GET` | `/api/patients/{id}/summary` | 获取 LLM 生成的摘要 |
| `POST` | `/api/upload/lab-result` | 上传化验报告（multipart） |
| `GET` | `/api/lab-results/{id}` | 获取提取的化验数据 |
| `POST` | `/api/lab-results/{id}/reprocess` | 重新运行提取 |
| `GET` | `/api/search` | 语义搜索 |

### API 调用时序图

```mermaid
sequenceDiagram
    autonumber
    participant U as 👨‍⚕️ 用户
    participant UI as 🖥️ Streamlit
    participant API as ⚡ FastAPI
    participant DB as 🐘 PostgreSQL
    participant S3 as 📦 MinIO
    participant AI as 🤖 Claude API
    participant VDB as 🔍 Milvus

    Note over U,VDB: 化验报告上传与提取流程
    
    U->>UI: 选择文件上传
    UI->>API: POST /api/upload/lab-result
    API->>S3: 存储原始文件
    S3-->>API: 文件路径
    API->>DB: 创建 LabResult (pending)
    API-->>UI: 返回 lab_result_id
    
    Note over API,AI: 后台异步处理
    
    API->>S3: 获取文件
    API->>AI: 发送图片/PDF 进行 VLM 提取
    AI-->>API: 返回结构化 JSON
    API->>DB: 更新 extracted_data
    API->>AI: 生成文本嵌入
    AI-->>API: 返回向量
    API->>VDB: 存储嵌入向量
    API->>DB: 更新状态 (completed)
    
    Note over U,VDB: 患者摘要生成流程
    
    U->>UI: 请求患者摘要
    UI->>API: GET /api/patients/{id}/summary
    API->>DB: 获取患者所有事件
    API->>AI: 发送时间线数据生成摘要
    AI-->>API: 返回摘要文本
    API-->>UI: 返回摘要
    UI-->>U: 显示 AI 摘要
```

---

## 化验报告处理流程

```mermaid
flowchart TD
    subgraph Upload["📤 1. 上传阶段"]
        A[用户上传文件] --> B{文件类型?}
        B -->|图片| C[存储到 MinIO]
        B -->|PDF| C
        C --> D[创建 LabResult<br/>status: pending]
    end
    
    subgraph Detection["🔍 2. 检测阶段"]
        D --> E{PDF 类型检测}
        E -->|文字版 PDF| F[pdfplumber 可提取]
        E -->|扫描图片/图片| G[需要 VLM 处理]
    end
    
    subgraph Extraction["🤖 3. 提取阶段"]
        F --> H[pdfplumber<br/>文本提取]
        G --> I[Claude VLM<br/>视觉提取]
        H --> J[结构化 JSON]
        I --> J
    end
    
    subgraph Validation["✅ 4. 验证阶段"]
        J --> K{Schema 验证}
        K -->|通过| L[验证成功]
        K -->|失败| M[标记失败<br/>需人工复核]
    end
    
    subgraph Storage["💾 5. 存储阶段"]
        L --> N[保存到 PostgreSQL]
        N --> O[生成嵌入向量]
        O --> P[存入 Milvus]
    end
    
    subgraph Complete["🎉 6. 完成"]
        P --> Q[更新状态: completed]
        M --> R[更新状态: failed]
        Q --> S[触发通知事件]
    end
    
    style Upload fill:#e3f2fd
    style Detection fill:#fff3e0
    style Extraction fill:#f3e5f5
    style Validation fill:#e8f5e9
    style Storage fill:#fce4ec
    style Complete fill:#e0f7fa
```

### 处理流程摘要

| 步骤 | 操作 | 工具/服务 |
|------|------|-----------|
| 上传 | 存储原始文件，创建 `pending` 状态 | MinIO, PostgreSQL |
| 检测 | 判断 PDF 是文字版还是扫描图片 | pdfplumber |
| 提取 | 文字 PDF 用 pdfplumber，图片用 VLM | pdfplumber, Claude |
| 验证 | 根据 Schema 验证提取的 JSON | Pydantic |
| 存储 | 保存结构化数据，生成嵌入向量 | PostgreSQL, Milvus |
| 事件 | 触发下游处理，更新状态 | FastAPI Events |

---

## UI 界面（Streamlit）

### 页面结构

```mermaid
flowchart LR
    subgraph Pages["📱 Streamlit 页面"]
        direction TB
        P1["🔍 患者搜索"]
        P2["📋 患者时间线"]
        P3["🧪 化验详情"]
        P4["📤 文件上传"]
        P5["🔐 登录"]
    end
    
    P5 --> P1
    P1 --> P2
    P2 --> P3
    P2 --> P4
    P4 --> P2
```

### 用户操作流程

```mermaid
journey
    title 泌尿外科医生日常使用流程
    section 登录系统
      打开平台: 5: 医生
      输入凭据: 3: 医生
      进入主页: 5: 医生
    section 查找患者
      搜索患者姓名/病历号: 5: 医生
      查看患者列表: 4: 医生
      选择患者: 5: 医生
    section 查看病史
      浏览时间线: 5: 医生
      查看 AI 摘要: 5: 医生, AI
      点击化验报告: 4: 医生
    section 上传新报告
      拖放文件上传: 4: 医生
      选择生命周期阶段: 3: 医生
      等待 AI 提取: 2: AI
      确认提取结果: 5: 医生
```

### 界面功能

| 页面 | 功能 | 关键组件 |
|------|------|----------|
| **患者搜索** | 搜索、筛选、快速预览 | 搜索框、筛选器、患者卡片 |
| **患者时间线** | 按生命周期的垂直时间线 | 时间线组件、AI 摘要面板 |
| **化验详情** | 原始文件与提取数据对比 | 并排显示、趋势图表 |
| **文件上传** | 拖放上传、状态追踪 | 上传区域、进度条、阶段选择 |

---

## 技术栈

| 组件 | 技术选型 |
|------|----------|
| 后端 | FastAPI |
| 数据库 | PostgreSQL + SQLAlchemy |
| 向量数据库 | Milvus |
| 文件存储 | MinIO (开发) / S3 (生产) |
| UI | Streamlit |
| VLM/LLM | Claude (claude-3-5-sonnet) |
| PDF 提取 | pdfplumber |
| 嵌入向量 | OpenAI/Voyage Medical |
| 认证 | JWT + python-jose |
| 后台任务 | FastAPI BackgroundTasks |
| 开发环境 | Docker Compose |

---

## 项目结构

```
urology-data-platform/
├── pyproject.toml
├── docker-compose.yml
├── src/
│   ├── main.py                 # FastAPI 入口
│   ├── api/                    # 路由（patients, events, upload, search, auth）
│   ├── models/                 # SQLAlchemy 模型
│   ├── schemas/                # Pydantic 模式
│   ├── services/               # 业务逻辑
│   │   ├── extraction/         # PDF + VLM 提取
│   │   ├── embedding.py        # Milvus 操作
│   │   ├── summary.py          # LLM 摘要
│   │   └── storage.py          # 文件存储
│   ├── core/                   # 配置、安全、数据库
│   └── workers/                # 后台任务
├── ui/                         # Streamlit 应用
│   ├── app.py
│   ├── pages/
│   └── components/
└── tests/
```

---

## 实施计划

### 项目里程碑

```mermaid
gantt
    title CUA数据库平台 MVP 开发计划
    dateFormat  YYYY-MM-DD
    
    section 阶段一：基础架构
    项目初始化           :a1, 2026-01-21, 2d
    Docker 环境配置      :a2, after a1, 1d
    核心模块开发         :a3, after a2, 2d
    数据模型设计         :a4, after a3, 2d
    数据库迁移配置       :a5, after a4, 1d
    
    section 阶段二：API 基础
    FastAPI 应用结构     :b1, after a5, 1d
    认证模块             :b2, after b1, 2d
    患者 CRUD API        :b3, after b2, 2d
    临床事件 API         :b4, after b3, 1d
    
    section 阶段三：上传与提取
    MinIO 存储服务       :c1, after b4, 1d
    文件上传端点         :c2, after c1, 2d
    PDF 文本提取         :c3, after c2, 2d
    VLM 图片提取         :c4, after c3, 3d
    后台任务处理         :c5, after c4, 2d
    
    section 阶段四：向量搜索
    Milvus 集成          :d1, after c5, 2d
    嵌入向量生成         :d2, after d1, 2d
    语义搜索端点         :d3, after d2, 2d
    LLM 摘要生成         :d4, after d3, 2d
    
    section 阶段五：UI 开发
    Streamlit 结构       :e1, after d4, 1d
    患者搜索页面         :e2, after e1, 2d
    时间线视图           :e3, after e2, 2d
    化验详情页面         :e4, after e3, 2d
    上传流程页面         :e5, after e4, 2d
    
    section 阶段六：测试部署
    单元测试             :f1, after e5, 2d
    集成测试             :f2, after f1, 2d
    部署文档             :f3, after f2, 1d
    MVP 发布             :milestone, f4, after f3, 0d
```

### 阶段详情

| 阶段 | 任务 | 产出 |
|------|------|------|
| **一：基础架构** | 项目初始化、Docker 配置、核心模块、数据模型、Alembic | pyproject.toml, docker-compose.yml, models/ |
| **二：API 基础** | FastAPI 结构、认证、患者 CRUD、临床事件 API | api/, schemas/, JWT 认证 |
| **三：上传与提取** | MinIO 服务、上传端点、PDF/VLM 提取、后台任务 | services/extraction/, workers/ |
| **四：向量搜索** | Milvus 集成、嵌入生成、语义搜索、LLM 摘要 | services/embedding.py, summary.py |
| **五：UI 开发** | Streamlit 页面、搜索、时间线、详情、上传 | ui/pages/, ui/components/ |
| **六：测试部署** | 单元测试、集成测试、部署文档 | tests/, docs/ |

---

## 部署架构

```mermaid
flowchart TB
    subgraph Dev["💻 开发环境"]
        direction LR
        DC["🐳 Docker Compose"]
        DC --> PG_D["PostgreSQL"]
        DC --> MV_D["Milvus"]
        DC --> MIO_D["MinIO"]
    end
    
    subgraph Prod["🏭 生产环境（医院内网）"]
        direction TB
        subgraph Compute["计算层"]
            K8S["☸️ Kubernetes / Docker Swarm"]
            K8S --> API_P["FastAPI Pod ×2"]
            K8S --> UI_P["Streamlit Pod ×1"]
        end
        
        subgraph Data["数据层"]
            PG_P["🐘 PostgreSQL<br/>(主从复制)"]
            MV_P["🔍 Milvus Standalone"]
            MIO_P["📦 MinIO / S3"]
        end
        
        subgraph Security["安全层"]
            LB["🔒 负载均衡器<br/>(SSL 终止)"]
            FW["🛡️ 防火墙"]
        end
        
        LB --> API_P
        LB --> UI_P
        API_P --> Data
    end
    
    subgraph External["☁️ 外部服务"]
        Claude["🤖 Claude API<br/>(经医院代理)"]
    end
    
    Prod <-->|HTTPS| External
    
    style Dev fill:#e3f2fd
    style Prod fill:#e8f5e9
    style External fill:#fff3e0
```

---

## 验证

### 本地开发

```bash
# 启动基础设施
docker-compose up -d

# 运行后端
uvicorn src.main:app --reload

# 运行 Streamlit
streamlit run ui/app.py
```

### 测试清单

1. 通过 API 或 UI 创建患者
2. 上传化验报告图片（血液检测照片）
3. 验证提取完成并在时间线中显示数据
4. 上传 PDF 化验报告
5. 验证提取完成
6. 查看患者摘要（LLM 生成）
7. 按化验值搜索患者
8. 查看特定检测的趋势图（如 PSA）

---

## 未来阶段（MVP 后）

```mermaid
timeline
    title CUA数据库平台 产品路线图
    
    section MVP
        2026 Q1 : 化验报告上传与提取
                : 患者时间线视图
                : AI 摘要生成
                : 语义搜索
    
    section 阶段二
        2026 Q2 : 影像模态支持 (CT/DICOM)
                : 超声图像集成
                : 内镜视频支持
    
    section 阶段三
        2026 Q3 : EMR 集成 (HL7/FHIR)
                : PACS 集成
                : 自然语言查询
    
    section 阶段四
        2026 Q4 : 临床决策建议
                : gRPC 微服务架构
                : 多站点部署
    
    section 长期
        2027+ : 完整 HIPAA 合规
              : 租户隔离
              : 跨站点联邦搜索
```

### 功能演进

| 阶段 | 核心功能 | 技术要点 |
|------|----------|----------|
| **MVP** | 化验报告、时间线、AI 摘要 | VLM 提取、LLM 摘要、向量搜索 |
| **阶段二** | 影像模态扩展 | DICOM 解析、视频处理 |
| **阶段三** | 系统集成 | HL7/FHIR 适配器、PACS 连接 |
| **阶段四** | 智能决策 | 临床建议、微服务化 |
| **长期** | 企业级部署 | 多租户、合规认证 |
